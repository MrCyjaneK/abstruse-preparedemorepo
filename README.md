# Abstruse Prepare Demo

This build should fail by default - there is no `neofetch' command in clean debian image, but after preparing the image we should see neofetch in our logs and a green build badge.
